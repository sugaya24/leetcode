import xorOperation from './1486.xor-operation-in-an-array';

describe('1486. XOR Operation in an Array', () => {
  test('1', () => {
    expect(xorOperation(5, 0)).toBe(8);
  });
  test('2', () => {
    expect(xorOperation(4, 3)).toBe(8);
  });
});
